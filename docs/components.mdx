# Component Documentation

This guide provides detailed information about all the React components available in Tangent Test.

## 🔢 Counter Component

The Counter component demonstrates state management with hooks and provides an interactive counting interface. This is why for some reason

### Features

*   **Increment/Decrement**: Adjust the counter value
    
*   **Custom Step Size**: Configure how much to increment/decrement
    
*   **Reset Functionality**: Return counter to zero
    
*   **Real-time Info**: Display counter properties (even/odd, positive/negative)
    

### Usage

```tsx
import Counter from './components/Counter'

function App() {
  return <Counter />
}

```

### Props

The Counter component doesn't accept any props - it manages its own internal state.

### State Management

```tsx
const [count, setCount] = useState(0)
const [step, setStep] = useState(1)

```

* * *

## 📝 TodoList Component

A comprehensive todo management system with filtering and priority features.

### Features

*   **Add Todos**: Create new tasks with priority levels
    
*   **Toggle Completion**: Mark tasks as complete/incomplete
    
*   **Delete Todos**: Remove unwanted tasks
    
*   **Filter System**: View all, active, or completed todos
    
*   **Priority Levels**: Low, Medium, High priority classification
    
*   **Statistics**: Real-time count of total, active, and completed tasks
    

### Usage

```tsx
import TodoList from './components/TodoList'

function App() {
  return <TodoList />
}

```

### Data Structure

```tsx
interface Todo {
  id: number
  text: string
  completed: boolean
  priority: 'low' | 'medium' | 'high'
}

```

### Key Functions

*   `addTodo()` - Creates new todo items
    
*   `toggleTodo(id)` - Toggles completion status
    
*   `deleteTodo(id)` - Removes todo from list
    

* * *

## 👤 UserCard Component

Displays user profile information fetched from an external API with navigation controls.

### Features

*   **API Integration**: Fetches data from JSONPlaceholder API
    
*   **User Navigation**: Previous/Next/Random user selection
    
*   **Loading States**: Shows loading spinner during data fetch
    
*   **Error Handling**: Displays error messages with retry functionality
    
*   **Responsive Design**: Adapts to different screen sizes
    

### Usage

```tsx
import UserCard from './components/UserCard'

function App() {
  return <UserCard />
}

```

### API Integration

```tsx
const fetchUsers = async () => {
  const response = await fetch('https://jsonplaceholder.typicode.com/users')
  const userData = await response.json()
  setUsers(userData)
}

```

### User Data Structure

```tsx
interface User {
  id: number
  name: string
  username: string
  email: string
  phone: string
  website: string
  company: {
    name: string
    catchPhrase: string
  }
  address: {
    street: string
    city: string
    zipcode: string
  }
}

```

## Component Architecture

All components follow these patterns:

### 🎨 Styling

*   CSS Modules for component-specific styles
    
*   Glass-morphism design with backdrop blur effects
    
*   Responsive design principles
    
*   Consistent color scheme and spacing
    

### 🔄 State Management

*   React hooks for local state
    
*   TypeScript interfaces for type safety
    
*   Proper error boundaries and loading states
    

### 🎯 Best Practices

*   Semantic HTML structure
    
*   Accessible form controls
    
*   Keyboard navigation support
    
*   Clean separation of concerns
    

## Customization

Each component can be easily customized by:

1.  **Modifying CSS variables** in the respective `.css` files
    
2.  **Extending interfaces** for additional data properties
    
3.  **Adding new features** following the established patterns
    
4.  **Integrating with external APIs** as demonstrated in UserCard